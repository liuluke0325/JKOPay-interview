# ADR-0003 — Frontend: Next.js (App Router) over plain React

**Date**: 2026-05-02
**Status**: Proposed

## Context

<!-- Filled during M3. Capture: why Next.js App Router specifically (SSR for demo,
     file-based routing for /, /search, /items/[id], easy Vercel deploy);
     what plain React + Vite would have looked like; why we chose this. -->

## Decision

<!-- Next.js (App Router) + TypeScript + Tailwind. -->

## Consequences

<!-- Easier deploy, free SSR, opinionated routing; cost is heavier setup
     and Next.js-specific quirks (RSC, server components vs client components). -->
