# Claude Code Instructions

**This project's agent rules live in [AGENTS.md](AGENTS.md). Read it at the
start of every session and follow it.** It is the shared source of truth used
by both Claude Code and Codex CLI; keeping a single file avoids drift between
the two tools.

If a Claude-specific override is ever needed, add it below this line and
reference the section of `AGENTS.md` it overrides.

<!-- Claude-specific overrides (none yet) -->
